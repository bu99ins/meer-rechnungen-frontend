# Local development: call the local Production backend (docker-compose at :5000) directly.
# The backend's CORS policy allows http://localhost:5173, so no Vite /api proxy is needed.
# Production builds set VITE_API_URL to the deployed backend HTTPS URL at build time instead.
VITE_API_URL=http://localhost:5000
